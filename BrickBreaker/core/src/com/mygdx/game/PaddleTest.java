package com.mygdx.game;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class PaddleTest {

    @Test
    void keep() {                  // testBallMoving (small initial, later capital   Ball ball = new Ball // notice captials @test ll pr ml
//        Paddle paddle = new Paddle(100, 20);        // Declare paddle as an instance of Paddle class
//        paddle.paddle_x = 23;                                   // Assign the paddl_x
//        paddle.keep(true, false,  640);      // Test the keep method
//        assertEquals(paddle.paddle_x, 17);               // assertEqual for paddle_x to input the expected value and compare with the actual value
    }
}